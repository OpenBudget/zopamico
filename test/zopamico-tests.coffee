chai = require 'chai'
sinon = require 'sinon'
_ = require '../bower_components/underscore'

{Zopamico} = require '../src/zopamico'

chai.should()

describe 'zopamico', ->
  it 'should do something', ->
    true.should.equal true